[project]
name = "math-viz"
version = "0.1.0"
description = "수능 수학 자동 시각화 해설"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "manim>=0.18.0",
    "pydantic>=2.0",
    "pyyaml>=6.0",
]

[tool.manim]
quality = "medium_quality"
frame_rate = 30
